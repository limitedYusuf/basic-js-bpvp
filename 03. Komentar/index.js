// ini komentar satu baris

/**
 * ini komentar multiple baris.
 *
 * @param {number} a - Angka pertama.
 * @param {number} b - Angka kedua.
 * @returns {number} Hasil penjumlahan a dan b.
 */
function tambah(a, b) {
   return a + b;
}